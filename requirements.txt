# Core dependencies for Internxt CLI
cryptography>=41.0.0
mnemonic>=0.20
requests>=2.31.0
click>=8.1.0
tqdm>=4.66.0

# WebDAV server dependencies
WsgiDAV>=4.3.0
waitress
# cheroot>=10.0.0 # is broken in this case atm for it cannot handle gracefully when a client opens a connection to the server but closes it immediately without sending any data

# Optional: for better SSL certificate handling
pyOpenSSL>=23.0.0

# Optional: TOTP code generation from a secret (for automated/Docker login)
pyotp>=2.9.0

# Development dependencies (optional)
# pytest>=7.4.0
# black>=23.0.0
# flake8>=6.0.0